function sameCase(a, b){
    let alphabet = ['abcdefghijklmnopqrstuvwxyz']
    for (let i = 0; i <= alphabet.length; i++){
       if (a = b){
      return 1
    }else if(a || b != alphabet.length){
      return -1
    }else if(a || b != alphabet.length){
      return -1
    }
  }